{
  "saveBook": [{
    "prerequisite": "#r['press'] == 'xxx'",
    "condition": "#r['discount'] >= 0.8",
    "message": {
      "id": "min.press.discount",
      "args": ["xxx", "0.8"]
    }
  }, {
    "prerequisite": "#r['press'] == 'yyy'",
    "condition": "#r['discount'] >= 0.7",
    "message": {
      "id": "min.press.discount",
      "args": ["yyy", "0.7"]
    }
  },{
    "condition": "#r['author'] != null",
    "message": {
      "id": "something.is.required",
      "args": ["作者"]
    }
  }, {
    "condition": "#r['name'] != null",
    "message": {
      "id": "something.is.required",
      "args": ["书名"]
    }
  }
  ]
}